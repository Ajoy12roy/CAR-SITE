# TODO: Fix Navbar.jsx Issues

- [x] Add missing `useState` import from React
- [x] Fix syntax error in className (mismatched quote and incorrect classes like `max-sm-h-screen` to `max-sm:h-screen`)
- [x] Move `menuLinks.map` inside the menu div for proper structure
- [x] Add hamburger menu button to toggle `open` state
- [x] Add conditional transform to menu div based on `open` state (e.g., translate-x-0 when open, -translate-x-full when closed)
- [ ] Test the component for mobile responsiveness
